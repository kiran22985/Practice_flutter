import 'package:flutter/material.dart';

class SingleViewPage extends StatelessWidget {
  const SingleViewPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
